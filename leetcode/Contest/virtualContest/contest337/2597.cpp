#include<bits/stdc++.h>
#include<algorithm>
#include<math.h>

using namespace std;

class Solution {
public:
    int beautifulSubsets(vector<int>& a, int k) {
        map<int,int>mp;
        mp[0]++;
        int n=a.size();
        for(int i=0;i<n;i++){
           
        }
        
    }
};