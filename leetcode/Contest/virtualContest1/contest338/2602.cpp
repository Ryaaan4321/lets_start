#include<bits/stdc++.h>
#include<algorithm>
#include<math.h>

using namespace std;

class Solution {
public:
    vector<long long> minOperations(vector<int>& a, vector<int>& q) {
        int n=a.size();
        int m=q.size();
        vector<long long>ans(q.size());
        
    }
};